# 약사 AI 챗봇 - 환경 변수 설정 예시
# 이 파일을 .env로 복사한 뒤 실제 값을 입력하세요:
#   copy .env.example .env

# OpenAI API 키 (필수)
OPENAI_API_KEY="your-openai-api-key"

# ChromaDB 경로 (선택 - 앱 사이드바에서도 설정 가능)
# CHROMA_DB_PATH=./chroma_db_combined_stored

# 임베딩/리랭커 모델 교체 실험용 (선택)
# EMBEDDING_MODEL_NAME=dragonkue/BGE-m3-ko
# RERANKER_MODEL_NAME=BAAI/bge-reranker-v2-m3
# (속도 튜닝) 기본 권장값
# RERANK_MAX_LENGTH=512
# RERANK_BATCH_SIZE=32

# 속도 개선이 미비할 때 모델 후보 (예시)
# EMBEDDING_MODEL_NAME=intfloat/multilingual-e5-base
# EMBEDDING_MODEL_NAME=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# RERANKER_MODEL_NAME=BAAI/bge-reranker-base

# Out-of-scope(문서 범위 밖 질문) 가드 설정
# OOS_GUARD_ENABLED=true
# OOS_MIN_RELEVANCE=0.55
# OOS_MIN_TOP_SCORE=0.002
# SOFT_FALLBACK_ENABLED=true   # 권장: OOS 시 딱딱한 거절 대신 친절한 일반 안내 문구 사용

# 응답 속도/품질 트레이드오프 설정
# (기본값: 속도 우선)
# USE_QUERY_OPTIMIZER=false
# CONTEXT_QUERY_ROUTER_ENABLED=true   # 문장 맥락 기반 검색어 자동 확장(규칙+LLM)
# VERIFY_MODEL=gpt-5.2
# RAGAS_MODEL=gpt-5.2
