# 약사 AI 챗봇 - 환경 변수 설정 예시
# 이 파일을 .env로 복사한 뒤 실제 값을 입력하세요:
#   copy .env.example .env

# OpenAI API 키 (필수)
OPENAI_API_KEY="your-openai-api-key"

# ChromaDB 경로 (선택 - 앱 사이드바에서도 설정 가능)
# CHROMA_DB_PATH=./chroma_db_combined_stored

# 임베딩/리랭커 모델 교체 실험용 (선택)
# EMBEDDING_MODEL_NAME=dragonkue/BGE-m3-ko
# RERANKER_MODEL_NAME=BAAI/bge-reranker-v2-m3

# Out-of-scope(문서 범위 밖 질문) 가드 설정
# OOS_GUARD_ENABLED=true
# OOS_MIN_RELEVANCE=0.55
# OOS_MIN_TOP_SCORE=0.002

# 외부 약학 API(OpenFDA) 연동 설정
# USE_EXTERNAL_API=true
# EXTERNAL_API_PROVIDER=openfda_multi   # 권장: 한국어 확장 키워드 다중 조회
# EXTERNAL_API_PROVIDER=openfda         # 단일 조회
# # EXTERNAL_API_PROVIDER=mfds_ezdrug     # MFDS 식약처 API (성능 문제로 사용 중지)
# # EXTERNAL_API_PROVIDER=korea_hybrid    # 식약처 + OpenFDA 멀티 결합(권장) - MFDS 관련 기능 사용 중지
# EXTERNAL_API_PROVIDER="openfda"  # "openfda", "openfda_multi" 권장
# EXTERNAL_FALLBACK_ONLY=true
# EXTERNAL_TRIGGER_MIN_RELEVANCE=0.55
# EXTERNAL_TRIGGER_MIN_TOP_SCORE=0.002
# EXTERNAL_TRIGGER_MODE=or   # or(권장): 둘 중 하나만 낮아도 호출 / and: 둘 다 낮아야 호출
# EXTERNAL_TOP_K=4
# EXTERNAL_TIMEOUT_SEC=8
# WEIGHT_EXTERNAL=0.2
# OPENFDA_API_KEY=your-openfda-api-key   # 선택(없어도 동작)
# MFDS_API_KEY=your-mfds-service-key      # 공공데이터포털 서비스키
# MFDS_EZDRUG_API_URL=https://apis.data.go.kr/1471000/DrbEasyDrugInfoService/getDrbEasyDrugList
# EXTERNAL_AUTO_TRANSLATE=true
# EXTERNAL_TRANSLATE_MODEL=gpt-5-nano
# EXTERNAL_TRANSLATE_MAX_CHARS=1800
# EXTERNAL_LLM_QUERY_ROUTER=true
# EXTERNAL_LLM_QUERY_ROUTER_MODEL=gpt-5-nano
# EXTERNAL_LLM_QUERY_ROUTER_MAX_KEYWORDS=6

# 응답 속도/품질 트레이드오프 설정
# (기본값: 속도 우선)
# USE_QUERY_OPTIMIZER=false
# VERIFY_MODEL=gpt-5.2
# RAGAS_MODEL=gpt-5.2
